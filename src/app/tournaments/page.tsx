import Link from "next/link";
import { ArrowRight, CalendarDays } from "lucide-react";
import { supabase } from "@/lib/supabase";
type Tournament={id:string;name:string;slug:string;organizer:string|null;status:string|null;location:string|null;prize_pool:number|null;participating_teams:number|null;start_date:string|null;end_date:string|null;verified:boolean|null};
type CountRow={tournament_id:string|null};const SIZE=10;
function num(value:unknown){const result=Number(value);return Number.isFinite(result)?result:0;}function pageNumber(value?:string){const page=Number.parseInt(value||"1",10);return Number.isFinite(page)&&page>0?page:1;}function date(value?:string|null){return value?new Date(value).toLocaleDateString("en-IN",{day:"2-digit",month:"short",year:"numeric"}):"TBD";}function money(value:unknown){const amount=num(value);if(!amount)return "TBD";if(amount>=10000000)return `₹${(amount/10000000).toFixed(1)}Cr`;if(amount>=100000)return `₹${(amount/100000).toFixed(1)}L`;return `₹${amount.toLocaleString("en-IN")}`;}
export default async function TournamentsPage({searchParams}:{searchParams?:Promise<{page?:string}>}){const params=searchParams?await searchParams:{};const page=pageNumber(params.page),offset=(page-1)*SIZE;const [eventsResult,matchesResult,standingsResult]=await Promise.all([supabase.from("tournaments").select("id,name,slug,organizer,status,location,prize_pool,participating_teams,start_date,end_date,verified",{count:"exact"}).order("created_at",{ascending:false}).range(offset,offset+SIZE-1),supabase.from("matches").select("tournament_id"),supabase.from("tournament_standings").select("tournament_id")]);if(eventsResult.error)return <main className="pr-container py-20"><h1 className="text-5xl font-semibold text-white">Tournaments could not be loaded.</h1></main>;const events=(eventsResult.data||[]) as Tournament[],matches=(matchesResult.data||[]) as CountRow[],standings=(standingsResult.data||[]) as CountRow[];const count=(rows:CountRow[],id:string)=>rows.filter((row)=>row.tournament_id===id).length,total=eventsResult.count||0,pages=Math.max(1,Math.ceil(total/SIZE));return <main className="bg-[var(--pr-bg)] text-white"><section className="border-b border-white/15"><div className="pr-container grid gap-12 py-16 md:py-24 lg:grid-cols-[1.2fr_.8fr] lg:items-end"><div><p className="pr-kicker">Events · organisers · competition</p><h1 className="mt-5 text-[clamp(4.5rem,9vw,9rem)] font-semibold uppercase leading-[.78] tracking-[-.08em]">Tournament<br/><span className="text-[var(--pr-red)]">register.</span></h1></div><div><p className="max-w-xl text-base leading-7 text-white/50">Track Indian esports events through dates, status, prize pools, match coverage and published standings.</p><div className="mt-7 flex gap-3"><Link href="/standings" className="pr-button pr-button-primary text-[10px]">Standings</Link><Link href="/matches" className="pr-button pr-button-secondary text-[10px]">Matches</Link></div></div></div></section><section className="border-b border-white/15"><div className="pr-container grid grid-cols-3">{[[total,"Events"],[events.filter((event)=>(event.status||"").toLowerCase().includes("live")).length,"Live on page"],[10,"Events per page"]].map(([value,label])=><div key={label} className="border-r border-white/15 px-5 py-7 first:border-l"><p className="text-2xl font-semibold">{value}</p><p className="mt-2 text-[9px] uppercase tracking-[.15em] text-white/25">{label}</p></div>)}</div></section><section className="pr-container py-16 md:py-22"><div className="flex items-end justify-between border-b border-white/15 pb-7"><div><p className="pr-kicker">Event index · page {page}</p><h2 className="mt-4 text-4xl font-semibold tracking-[-.05em] md:text-6xl">Competition calendar.</h2></div><CalendarDays size={20} className="text-[var(--pr-red)]"/></div><div>{events.length?events.map((event)=><Link key={event.id} href={`/tournaments/${event.slug}`} className="pr-ranking-row group grid gap-5 border-b border-white/10 py-6 md:grid-cols-[160px_1.4fr_repeat(3,.6fr)_24px] md:items-center"><div><p className="font-semibold">{date(event.start_date)}</p><p className="mt-1 text-[9px] uppercase tracking-[.14em] text-white/25">to {date(event.end_date)}</p></div><div><p className="text-lg font-semibold">{event.name}</p><p className="mt-1 text-[9px] uppercase tracking-[.14em] text-white/25">{event.organizer||"Organiser TBD"} · {event.location||"India"}</p></div><Stat value={event.status||"Upcoming"} label="Status"/><Stat value={money(event.prize_pool)} label="Prize pool"/><Stat value={`${count(matches,event.id)} / ${count(standings,event.id)}`} label="Matches / rows"/><ArrowRight size={15} className="hidden text-white/20 group-hover:text-[var(--pr-red)] md:block"/></Link>):<p className="py-14 text-white/35">No tournaments found.</p>}</div><nav className="mt-8 flex items-center justify-between border-t border-white/15 pt-7"><p className="text-[10px] uppercase tracking-[.16em] text-white/30">Page {Math.min(page,pages)} of {pages}</p><div className="flex gap-2">{page>1?<Link href={`/tournaments?page=${page-1}`} className="pr-button pr-button-secondary text-[10px]">Previous 10</Link>:null}{page<pages?<Link href={`/tournaments?page=${page+1}`} className="pr-button pr-button-primary text-[10px]">Next 10 <ArrowRight size={13}/></Link>:null}</div></nav></section></main>;}function Stat({value,label}:{value:string|number;label:string}){return <div><p className="truncate text-sm font-semibold">{value}</p><p className="mt-1 text-[9px] uppercase tracking-[.14em] text-white/25">{label}</p></div>;}
